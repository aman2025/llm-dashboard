# Telemetry Node Logging
Monitors platform indicators under real-time conditions:
- Default local monitoring port: `9090`
- Logs local hardware temperatures similar to `macmon` metrics.
If port binding conflicts, see `troubleshooting.md` to clear bind indexes.
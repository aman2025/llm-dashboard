---
description: Stage all files and commit the changes
---

Stage all files with `git add -A` and commit. If the user provided context after `/cmt`, use it verbatim as the commit message. and do not run `git status`, `git log`, format, or lint.
